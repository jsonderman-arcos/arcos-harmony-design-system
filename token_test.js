const fs = require("fs"); const path = require("path"); const tokens = JSON.parse(fs.readFileSync("tokens/coreTokens.json", "utf8")); console.log("Font family:", tokens.lighthouse.typography["fontfamily-base"]); const fontFamily = tokens.lighthouse.typography["fontfamily-base"]; console.log("$value:", fontFamily.$value); console.log("$rawValue:", fontFamily.$rawValue || "undefined"); console.log("Font size:", tokens.lighthouse.typography["fontsize-base"]);

const path = require("path"); const fs = require("fs"); const buttonThemeMapping = require("./scripts/button-theme-mapping"); const tokens = JSON.parse(fs.readFileSync(path.join(__dirname, "tokens/lightModeTokens.json"), "utf8")); const coreTokens = JSON.parse(fs.readFileSync(path.join(__dirname, "tokens/coreTokens.json"), "utf8")); const combinedTokens = { ...coreTokens, ...tokens }; console.log("Button theme mapping keys:", Object.keys(buttonThemeMapping)); function processTheme(theme, tokens) { return JSON.parse(JSON.stringify(theme)); } const modeTheme = processTheme(buttonThemeMapping, combinedTokens); console.log("Mode theme keys:", Object.keys(modeTheme)); console.log("Does modeTheme have colorSchemes?", modeTheme.colorSchemes !== undefined);

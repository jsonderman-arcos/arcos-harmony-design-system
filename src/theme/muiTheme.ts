// muiTheme.ts: Import Style Dictionary output and create MUI theme
import { createTheme } from '@mui/material/styles';

const muiTheme = createTheme({
  // Add palette and other theme config here
});

export default muiTheme;
